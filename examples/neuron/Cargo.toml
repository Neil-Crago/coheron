[package]
name = "neuron"
version = "0.1.4"
authors = ["Neil Crago"]
edition = "2024"
description = "A simple neuron-like belief tracking example using the local coheron crate"
keywords = ["neuron", "simulation", "coheron", "beliefs", "fusion"]

[dependencies]
coheron = { path = "../.." }
