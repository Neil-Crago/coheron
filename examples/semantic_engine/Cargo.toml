[package]
name = "semantic_engine"
authors = ["Neil Crago"]
version = "0.1.0"
edition = "2024"
description = "A fusion-loop example using the local coheron library"

[dependencies]
coheron = { path = "../.." }